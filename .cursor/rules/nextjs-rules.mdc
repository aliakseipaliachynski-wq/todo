---
description: >
  Best practices for Next.js (App Router).
  Encourages correct usage of Server and Client Components
  and clean data fetching patterns.
globs:
  - "app/**/*.ts"
  - "app/**/*.tsx"
alwaysApply: true
---

# Next.js Rules

- Server Components by default.
- Use `"use client"` only when required.
- Fetch data in Server Components when possible.
- Avoid duplicate data fetching.
- No business logic in `page.tsx` or `layout.tsx`.
