# Update Todo Feature

Renders a single todo with checkbox (toggle completion), inline edit on double-click, and delete with confirmation.

## Usage

```tsx
<TodoItem
  todo={todo}
  onUpdate={updateTodo}
  onDelete={deleteTodo}
/>
```

## Files

- `ui/todo-item.tsx` – Todo row component
- `index.ts` – Public exports
